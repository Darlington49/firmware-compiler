name: learn-github-actions
on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: docker://espressif/idf:release-v4.0
    - name: Build the Docker image
      run: idf.py build
